name: Validate Bicep Infrastructure File

on:
  workflow_call:
    inputs:
      AZURE_RG_NAME:
        required: true
        type: string
      AZURE_RG_LOCATION:
        required: true
        type: string

    secrets:
      AZURE_CREDENTIALS:
        required: true

jobs:
  create:
    name: Create a resource group
    runs-on: ubuntu-latest

    steps:
      - uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
          enable-AzPSSession: true

      - name: Check and create
        uses: AzureCLI@2
        with:
          failOnStandardError: true
          inlineScript: |
            #!/bin/bash
            echo "Checking resource group ${{ inputs.AZURE_RG_NAME }}"
            if $(az group exists --name ${{ inputs.AZURE_RG_NAME }}) ; then
              echo "Azure resource group already exists, skipping creation..."
            else
              az group create --name ${{ inputs.AZURE_RG_NAME }} --location ${{ inputs.AZURE_RG_LOCATION }} --tags ""
              echo "Azure resource group created"
            fi
            exit 0
